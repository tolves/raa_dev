<div>
  <table>
    <tr>
      <td>
        <%= link_to :Index, :root %>
      </td>
      <td>
        <% if current_user.is_admin? %>
          <h1>You are Logged In, <%= current_user.name %></h1>
          <%= button_to 'Sign Out', :admin_logout, method: :post %></td>
        <% end %>
        <td>
          <%= button_to :Admin_Dashboard, :admin, method: :get %>
        </td>
        <td>
          <%= button_to :admin_products_path, :admin_products, method: :get %>
        </td>
        <td>
          <%= button_to :users_path, :users, method: :get %>
        </td>
        </tr>
  </table>
</div>